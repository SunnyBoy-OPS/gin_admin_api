# 系统配置
system:
  host: "0.0.0.0"
  port: 5001
  env: debug
  # env: release

#logger日志
logger:
  lever: info
  prefix: '[gin-admin-api]'
  director: logger
  show_line: true
  log_in_console: true

# 数据库mysql配置
mysql:
  host: 10.0.0.105
  port: 3306
  db: ginblog
  username: root
  password: root
  log_level: dev
  charset: utf8
  maxIdle: 50
  maxOpen: 150
